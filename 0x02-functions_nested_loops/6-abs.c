#include "main.h"

/**
 * _abs - function that computes thr absolute value of an integer
 *
 * @n: parametr
 *
 * Return: return to 0
 */

int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
